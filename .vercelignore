# 루트 기준 앵커(/) 필수 — 'supabase/'처럼 쓰면 lib/supabase까지 매칭돼 빌드가 깨진다
# Python worker (FLAME 자산 56MB 포함) — Modal에 별도 배포됨
/services/
# 로컬 테스트 사진
/image/
# 문서/DB 마이그레이션 (배포 불필요)
/docs/
/supabase/
/.agents/
# 로컬 환경 변수 (Vercel 환경 변수로 별도 등록)
.env.local
.env.local.*
